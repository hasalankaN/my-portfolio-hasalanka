"use client";

import type { SidebarNavItem } from "@/data/sidebar";

interface ReferralNavbarProps {
  filteredSidebarData: SidebarNavItem[];
}

export default function ReferralNavbar({ filteredSidebarData: _ }: ReferralNavbarProps) {
  return null;
}
